# Data Contract

Build: `YYYYMMDD-NN`

## Storage

使用する保存先、キー、テーブル、シート等を記載する。

## API

主要な入出力形式を記載する。

## Compatibility

- 既存フィールドは原則削除・改名しない。
- フィールド追加後も旧データを読み込める状態を維持する。
- DB構造変更はmigrationとして残す。

保存形式・API仕様を変更した場合は、このファイルをコードと同じ変更単位で更新する。
